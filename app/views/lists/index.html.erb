<h1>Merlin's Magic To-Do List Maker</h1>

<h3>Make as many To-Do lists as your little heart desires</h3>

<table>
  <tr>
    <th>List Names</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @lists.each do |list| %>
  <tr>
    <td><%= link_to list.name, list %></td>
    <td><%= link_to 'Edit', edit_list_path(list) %></td>
    <td><%= link_to 'Destroy', list, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New List', new_list_path, :class => 'btn primary' %>
