<p id="notice"><%= notice %></p>

<h2><%= @list.name %></h2>

<%= link_to 'New Item', new_list_task_path(@list) %>

<%# @list.each do |i| %>
    

    
<%# end %>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
